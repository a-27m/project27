---
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  dotnet:
    name: .NET
    uses: ./.github/workflows/build-dotnet.yaml
    with:
      service-name: project27

  web:
    name: Web
    runs-on: ubuntu-latest
    timeout-minutes: 15
    outputs:
      result: ${{ steps.test.outcome }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 26.x
          cache: 'npm'
          cache-dependency-path: './web/package-lock.json'

      - name: Set up .NET
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: '10.0.x'

      - name: Install dependencies
        working-directory: ./web
        run: npm ci

      - name: Build
        working-directory: ./web
        run: npm run build --if-present

      - name: Lint
        working-directory: ./web
        run: npm run lint

      - name: Test
        id: test
        working-directory: ./web
        run: npm test

      - name: Install Playwright browsers
        working-directory: ./web
        run: npx playwright install --with-deps chromium

      - name: E2E
        working-directory: ./web
        run: npm run e2e

      - name: Upload Playwright report
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: playwright-report
          path: web/playwright-report
          retention-days: 7
          if-no-files-found: ignore

  semver:
    name: Version
    if: success() && github.ref == 'refs/heads/main'
    needs: [dotnet, web]
    uses: ./.github/workflows/semver-tag.yaml
    permissions:
      contents: write
    with:
      service-name: project27

  docker-backend:
    name: Docker Backend
    uses: ./.github/workflows/ci-docker-template.yaml
    needs: [dotnet, semver]
    with:
      service-name: 'project27-server'
      dockerfile-path: './src/Project27.Server/Dockerfile'
      version: ${{ needs.semver.outputs.version }}
    permissions:
      contents: read
      packages: write
    secrets: inherit

  docker-web:
    name: Docker Web
    uses: ./.github/workflows/ci-docker-template.yaml
    needs: [web, semver]
    with:
      service-name: 'project27-web'
      dockerfile-path: './web/Dockerfile'
      build-context: './web'
      version: ${{ needs.semver.outputs.version }}
    permissions:
      contents: read
      packages: write
    secrets: inherit

  docker-mcp:
    name: Docker MCP
    uses: ./.github/workflows/ci-docker-template.yaml
    needs: [dotnet, semver]
    with:
      service-name: 'project27-mcp'
      dockerfile-path: './src/Project27.Mcp/Dockerfile'
      version: ${{ needs.semver.outputs.version }}
    permissions:
      contents: read
      packages: write
    secrets: inherit

  helm:
    name: Helm
    uses: ./.github/workflows/helm.yaml
    needs: [docker-backend, docker-web, docker-mcp]
    permissions:
      contents: write
      packages: write
